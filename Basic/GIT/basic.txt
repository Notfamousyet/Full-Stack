Git and Github are different.

Git(software): The tool on your computer that saves your history.

GitHub(service): The website where you store and share that history with others.

Commit -> checkpoint

In powershell

-- To create multiple folder
    mkdir git1,git2,git3  

-- To initiate track go to that path and use used to create 
    git init

-- To check status of it.
    git status

-- To create File we use
    ni git1.txt

-- To save and exit click Esc and wrire :wq and changes occur 

-- git add .
(all files get added)

-- To directly add message without code editor
git commit -m "hello"

-- To check logs (particular id and details)
git log
git log --oneline

use ctrl + shift + P and type install command code in path

-- In git there we can create a .gitignore file and whatever file or folder we mention in it git will not keep track of it.

-- Each commit refers to previous hash commit and 1st one  to null.

-- To know branching use
git branch

-- To create a new branch use keywords and add name to it. 
git branch abc

-- To switch branch and to create and switch. 
git checkout abc
git switch -c abc

-- To merge any branch to main 
git merge abc

-- To delete any branch
git branch -d abc

-- To see difference between what previous and after. Here '-' and '+' are used to differentiate what changes are made.To quit only write :q
git diff --staged

-- To switch without commit and having and update so you can switch branch
git stash

-- To get those changes back use 
git stash pop

-- using stash command we can take back changes in different branches.

-- To directly add and commit 
git commit -am "message to write"

-- To rebase use command is given below even understand while using it that all hash will change then others not able to pull it using there hashs.Try not to do it on master branch.
git rebase master

-- If conflicts are there then make changes in file save then use this two commands.
git add abc
git rebase --continue

-- To check added resourse 
git remote -v 

-- If have to add to origit
git remote add origin https://githubabc.git

-- To rename it abc is older and def is new and to remove
git remote rename abc def
git remote remove def

-- To push it to origin and in master branch
git push -u origin master

-- To push after running upper thing then you can directly use 
git push

-- To install loccally use command
git clone https://abc.com

-- To get into but dont want to put in workspace
git fetch

-- To grt info and add it in your workspace (git pull= git fetch + git merge)
git pull

-- To want to update open source 
1) go to fork add it then clone repository to you local.
2) create new branch on origin then make changes as needed
3) push it to orgin that branch you created
4) go to github add description on pop up pull request if they agree then they will commit and merge.
